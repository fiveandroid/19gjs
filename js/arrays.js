// object --> Array


/*

    let newArray = [1, 2, 3, 'str', [1,2] ]
    console.log( newArray )

    // .length --> tamaño del array --> Cantidad de elementos que contiene

    console.log( newArray[3] )
    console.log( newArray[4][1])

    for(i=0; i<newArray.length; i++){
        console.log( newArray[i])
    }

    arrayDeNumeros.forEach( numero, index, array)=>{

    })

    Metodos de Array

    // .push() --> Agregar el elemento al array
    // .pop() --> Elimina item del array
*/

/*

    const strReverseMedium = (str) => {
        return str.split('').reverse().join('')
    }

    const strReverseMedium = str => {  str.split('').reverse().join('')  }

    //funcion simplificada
    const strReverseMedium = str =>  str.split('').reverse().join('')  


    const obtenerPares = ( arreglodenumeros) =>{

        arrayDeNumeros.forEach( (numero, index, array)=>{

            if (numero % 2 === 0){
                console.log( `El numero ${numero} es par`)
            }

        })
    }
*/